// src/components/GeneradorContenidosPage.jsx

import React from 'react';

// Estilos básicos en línea para empezar
const styles = {
  container: {
    padding: '2rem',
    textAlign: 'center',
  },
  title: {
    color: '#1b5784',
  }
};

function GeneradorContenidosPage() {
  return (
    <div style={styles.container}>
      <h1 style={styles.title}>Generador de Contenidos (Temarios)</h1>
      <p>Esta sección está en construcción.</p>
      {/* Aquí es donde eventualmente irá el componente del generador de temarios */}
    </div>
  );
}

export default GeneradorContenidosPage;
